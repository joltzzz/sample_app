<% form_for([@post, @post.comments.build]) do |f| %>
    <p>
        <% f.label :content %> <br />
        <% f.text_area :content %>
    </p>
    <br />
    <p>
        <% f.submit, class: "btn btn-primary" %>
    </p>
<% end %>